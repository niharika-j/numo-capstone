import React from 'react';
import './Prototypes.scss';

class Prototypes extends React.Component {
  render() {
    return (
      <div>
        Loaded Prototypes page
      </div>
    );
  }
}

export default Prototypes;
